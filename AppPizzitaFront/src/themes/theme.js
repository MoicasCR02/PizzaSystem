import { createTheme } from "@mui/material/styles";
export const appTheme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#8a1617',
    },
    secondary: {
      main: '#e8b838',
    },
  },
});
